import React from "react";
import type { InputProps } from "./input.types";
import "./input.css";
export declare function Input({ variant, size, label, helperText, errorMessage, leftIcon, rightIcon, required, className, wrapperClassName, id, disabled, ...props }: InputProps): React.JSX.Element;
